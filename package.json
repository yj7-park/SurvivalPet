{
  "name": "survival-sim",
  "version": "0.38.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:pages": "tsc --noEmit && vite build --config vite.config.pages.ts",
    "preview": "vite preview",
    "preview:pages": "vite preview --outDir dist-pages"
  },
  "dependencies": {
    "firebase": "^10.12.0",
    "phaser": "^3.80.1",
    "seedrandom": "^3.0.5",
    "simplex-noise": "^4.0.3"
  },
  "devDependencies": {
    "@types/seedrandom": "^3.0.8",
    "typescript": "^5.4.5",
    "vite": "^5.2.12"
  }
}
